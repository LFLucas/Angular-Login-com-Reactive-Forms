import { TrackStatusDirective } from './track-status.directive';

describe('TrackStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackStatusDirective();
    expect(directive).toBeTruthy();
  });
});
